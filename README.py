import Profile from "RUFFYpy"

class Bio extends Profile { 
name = "RUFFY"; 
email = "nonya@buizzz.net"; 
website = "https://notexisting.beansoup"; }

class noSkills extends Profile { 
languages = ["whole lotta nothing"]; 
frontend = ["licor"]; 
backend = ["poker"]; desktop = ["too expensive"]; 
databases = ["yo girls i-messages"]; 
devOps = ["Got no ops but stay active"]; 
toolchains = ["glizzy"]; 
hosting = ["I cook you dinner babe, but don't spill the bean soup"]; 
operatingSystems = ["cross faded"]; 
editors = ["the one that works for now"]; }
